# 全局 WebSocket 升级映射（必须位于 http{} 上下文，且全 nginx 只此一份）。
# conf.d/*.conf 被 include 在 http{} 内，故放这里生效。
# 用法：location 里 proxy_set_header Connection $connection_upgrade;
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
