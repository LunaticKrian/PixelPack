README.md
*.md
docker-compose.yml
.dockerignore
.git

# 新项目模板仅作参考，不能被 nginx 加载——否则启动时会把
# <project>-api / <project>.airise.site 当成真实 upstream 去解析，
# 报 "host not found in upstream" 导致网关崩溃重启。接入新项目时
# 复制它另存为 <project>.airise.site.conf，本文件保持不进镜像。
conf.d/_template.conf
